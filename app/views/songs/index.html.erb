<h1>All Songs</h1>

<h2><%= pluralize(@songs.count, 'song')%></h2>

<ul>
  <% @songs.each do |song| %>
    <li><%= link_to("#{song.artist.name} - #{song.title}", song_path(song)) %></li>
  <% end %>
</ul>
