<h1><%= @artist.name.capitalize %>'s songs:</h1>
<ul>
<% @artist.songs.each do |song| %>

  <li><%= link_to song.title, song_path(song) %></l>

<% end %>
</ul>

<%= link_to "Artist Index", artists_path %>
